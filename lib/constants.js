const HTML_TAG = '<html';

const HTML_TAG_LENGTH = HTML_TAG.length;

const CONTENT_TYPE_HEADER = 'content-type';

const CONTENT_LENGTH_HEADER = 'content-length';

const CONTENT_TYPE_VALUE = 'text/html; charset=utf-8';

const DOCTYPE_HTML = '<!doctype html>';

module.exports = {
  HTML_TAG,
  HTML_TAG_LENGTH,
  DOCTYPE_HTML,
  CONTENT_LENGTH_HEADER,
  CONTENT_TYPE_HEADER,
  CONTENT_TYPE_VALUE
};
